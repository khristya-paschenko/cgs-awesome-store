model Product {
  id            String   @id @default(uuid()) @db.Uuid
  name          String
  description   String
  stock         Int
  price         Float
  category      String
  orderDetails  OrderDetail[]
}
